import React, { Component } from "react";
import loginAction from "../actions/API";

export default class Login extends Component {

  render() {
    return (
      <div>
        <div>Login Page</div>
        <form action=""></form>
      </div>
    );
  }
}
